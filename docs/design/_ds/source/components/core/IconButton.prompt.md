Use `IconButton` in toolbars, transports and filter rows.

```jsx
<IconButton label="Filter markers"><FilterIcon /></IconButton>
<IconButton label="Play" active><PlayIcon /></IconButton>
```

Always pass `label`. Bump `size` to 44 anywhere touch matters.