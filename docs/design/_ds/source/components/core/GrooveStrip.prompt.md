Use `GrooveStrip` when grooves are needed without a full `Surface` — most often as a page-level section divider.

```jsx
<GrooveStrip divider count={2} style={{ height: 34 }} />
```

Prefer `Surface` for anything that is a panel or card; this is the loose primitive.