Use `Tag` for session IDs, readings and states.

```jsx
<Tag tone="live" dot>LIVE</Tag>
<Tag tone="alert">ABSENCE 6.4s</Tag>
<Tag>SESSION_047</Tag>
```

Content is always uppercase and always factual. `dot` only when something is actually live — a decorative pulse trains people to ignore it.