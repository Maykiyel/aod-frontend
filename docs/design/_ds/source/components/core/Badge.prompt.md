Use `Badge` inside `NotchedCard`'s `badge` slot, or inline to mark a comm type.

```jsx
<Badge tone="var(--compound)"><Diamond /></Badge>
```

Hollow by default so it reads as a machined boss sitting in the notch, not a sticker.