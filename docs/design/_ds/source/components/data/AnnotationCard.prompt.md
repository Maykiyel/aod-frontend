Use `AnnotationCard` in the review rail and anywhere notes are listed.

```jsx
<AnnotationCard author="Coach Vela" timestamp="14:22" body="Call lands after the rotate is already committed." />
```

One step of elevation when selected — E2 to E3, never straight to E4.