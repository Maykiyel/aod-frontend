Use `SectionHeader` at the top of every documentation or landing section.

```jsx
<SectionHeader eyebrow="Foundations" index="03/09" title="Elevation" lede="Five levels on a near-black substrate." />
```

The 5px red square before the eyebrow is a fixed part of the mark — keep it.