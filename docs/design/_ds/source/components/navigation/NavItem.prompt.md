Use `NavItem` for sidebar rows.

```jsx
<NavItem label="Sessions" active icon={<ApertureIcon />} />
<NavItem label="Team" icon={<RoleIcon />} />
```

Exactly one active row. The red left edge is the only red in the sidebar.